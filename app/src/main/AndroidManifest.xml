<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ajou.ourvillage">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".kakaoApi"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OurVillage"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".Apart.ApartHelpWritePostActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".Apart.ApartCommunityActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".Apart.ApartHelpActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".Apart.ApartNoticeActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".Login.SignUpActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="@string/kakao_app_key" />

        <activity
            android:name=".Login.LoginActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Design.NoActionBar"></activity>
    </application>

</manifest>